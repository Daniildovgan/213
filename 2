#include <iostream>
#include <cmath>

int main() {
    const double pi = 3.14;
    double L;
    
    std::cout << "Введіть довжину кола: ";
    std::cin >> L;

    double R = L / (2 * pi);
    double S = pi * pow(R, 2);

    std::cout << "Площа кола: " << S << std::endl;

    return 0;
}
